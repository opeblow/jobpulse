# Copy to .env.local and fill in your keys.
# The same keys must be set on your Convex deployment (npx convex env set ...).

OPENAI_API_KEY=
FIRECRAWL_API_KEY=
AGENTMAIL_API_KEY=
VITE_CONVEX_URL=

# Optional:
# OPENAI_MODEL=gpt-4o-mini
